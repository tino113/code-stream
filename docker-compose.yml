services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codestream-app
    ports:
      - "${CODESTREAM_PORT:-8124}:8000"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
