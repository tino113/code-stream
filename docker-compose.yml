services:
  api:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: >-
      bash -lc "pip install -r /app/requirements.txt &&
      uvicorn backend.app.main:app --host 0.0.0.0 --port 8124 --reload"
    ports:
      - "8124:8124"
